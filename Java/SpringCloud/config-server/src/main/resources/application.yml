server:
  port: 8766

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8760/eureka

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sulvto
          searchPaths: respo
          username: sulvto
          password:
      label: master